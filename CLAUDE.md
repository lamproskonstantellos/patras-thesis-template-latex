# Repo conventions for Claude Code

## Git commit policy
- Author and committer must always be:
  `Lampros Konstantellos <58119782+lamproskonstantellos@users.noreply.github.com>`
- NEVER add `Co-Authored-By: Claude <noreply@anthropic.com>` (or any
  Anthropic / Claude) trailers to commits.
- NEVER add `🤖 Generated with Claude Code` footers, or any other
  Anthropic / Claude attribution lines.
- NEVER include links to `claude.ai`, `claude.ai/code`, `anthropic.com`,
  or any session URLs (e.g. `https://claude.ai/code/session_...`)
  in commit messages.
- NEVER reference "Claude", "Claude Code", "AI assistant", or similar
  in commit messages.
- Commit messages end at the last meaningful line. Nothing else after.

## Pull requests, issues, and comments
- NEVER add `Generated by Claude Code` (or similar) footers to PR
  descriptions, issue bodies, or comments.
- NEVER include `https://claude.ai/...` or `https://anthropic.com/...`
  links anywhere in PR/issue/comment text.
- NEVER include Anthropic / Claude attribution in titles or bodies.
- Write all PR descriptions, issue bodies, and comments as if I
  authored them myself.

## Branch naming
- NEVER prefix branches with `claude/`.
- Use `feature/...`, `fix/...`, `chore/...`, `docs/...` based on intent.
